{% extends 'base.html.twig' %}
  {% block title %}Detalles de la investigacion "{{ investigacion.Titulo| capitalize }}"{% endblock %}
    {% block stylesheets %}
        {{ parent() }}
    {% endblock %}
{% block body %}
    {% block navegacion %}
        {{ parent() }}
    {% endblock %}
    <body style="background-color: #002E94">
    <div style="width: 1000px;height:500px;margin-top: 60px;
margin-left:40px;border-style: solid;background:#f2e7c3;" %}>
        <h2>"{{ investigacion.Titulo| capitalize }}"</h2>
        <p>{{ investigacion.descripcion }}</p>
        <h2 id="Catedra">{{ investigacion.Catedra }}</h2>
        {% if is_granted('ROLE_JURADO') %}
        {% if investigacion.puntuacion!=null %}
            <p>{{ investigacion.puntuacion }}</p>
        {% else %}
            <div class="m-auto p-2 container-fluid">
                <button id="evaluar"> Dar evaluacion</button>
                <br>
                <div id="escondido">
                    <select id="Premio" class="my-2 p-1">
                        <option> Relevante</option>
                        <option> Destacado</option>
                        <option> Mencion</option>
                        <option> Sin Premio</option>
                    </select>
                    <br>
                    <button id="Subir" onclick="Evaluar({{ investigacion.id }},o)" type="submit">Evaluar</button>
                </div>
            </div>
        {% endif %}
        {% endif %}
       {#  <a href="{{ asset('uploads/'+investigacion.archivo) }}">{{ investigacion.archivo }}</a>
       #}<footer style="color: #0a58ca;margin-top: 360px; padding-right: 2%; float: left ">publicado por</footer>
    </div>
    </body>
    {% block javascripts %}
        {{ parent() }}
        <script src="{{ asset('js/Evaluacion.js') }}"></script>
    {% endblock %}
{% endblock %}